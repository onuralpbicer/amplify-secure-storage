# amplify-secure-storage

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build amplify-secure-storage` to build the library.

## Running unit tests

Run `nx test amplify-secure-storage` to execute the unit tests via [Vitest](https://vitest.dev/).
